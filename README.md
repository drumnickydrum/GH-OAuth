# GitHub OAuth with React and Express

## Installation

Terminal 1:

```bash
cd server && npm i
```

Terminal 2:

```bash
cd client && npm i
```

## Start

Terminal 1:

```bash
cd server && npm start
```

Terminal 2:

```bash
cd client && npm start
```
